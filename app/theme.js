import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    background: {
      default: "#ede2c8", 
    },
    primary: {
      main: '#57B8FF',
      dark: '#363635',
      contrastText: '#fff',
    }
  },
});
export default theme;